[project]
name = "sample"
version = "0.2.0"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
]

[project.optional-dependencies]
cli = [
    "typer>=0.12",
]

[project.scripts]
serve = "sample.cli:serve"

[dependency-groups]
dev = [
    "pytest>=8",
]
